# Core API Framework
fastapi==0.104.1
uvicorn==0.24.0

# Database
pymongo==4.6.0

# Authentication & Security
python-jose==3.3.0          # used in routers/auth.py (from jose import JWTError, jwt)
PyJWT==2.8.0                # used in jwt_config.py (import jwt)
passlib==1.7.4
bcrypt==4.1.1

# Request Parsing
python-multipart==0.0.6

# Data Validation
pydantic==2.5.0
email-validator==2.1.0      # required for pydantic EmailStr in models.py

# HTTP Client
requests==2.31.0

# Environment Variables
python-dotenv==1.0.0

# Static File Serving (needed by FastAPI StaticFiles)
aiofiles==23.2.1
